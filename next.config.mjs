/** @type {import('next').NextConfig} */
const nextConfig = {
  images: { domains: ["encrypted-tbn0.gstatic.com", "cloudflare-ipfs.com"] },
  // images: { remotePatterns: ["encrypted-tbn0.gstatic.com"] },
};

export default nextConfig;
